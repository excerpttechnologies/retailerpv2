/* Form fields for All Sales Term Master.
   Lives beside the pages that use it - not in a global registry. */

export const FIELDS = [
    { k: "name", label: "Name", type: "text", req: true },
  ];
