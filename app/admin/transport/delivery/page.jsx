'use client';
import DeliveryView from '@/components/DeliveryView';

/* Delivery / LR Transactions - /admin/transport/delivery

   Filter card, list, Add / Edit dialog and the read-only View dialog all
   live in one view, matching the deployed screen. */

export default function DeliveryPage() {
  return <DeliveryView />;
}
